/**
 * class that can have one of the next direction: up, down, right, left
 */
public enum EnumDirection {
    // modify to the specific way of the board
    // possible direction are: UP, DOWN, RIGHT, LEFT
    // should this class contain only one string that will be the direction?
    UP, DOWN, RIGHT, LEFT;
}
